function oddEven (num){
    if(isNan (num)) || typeof parseInt (num) !== "number") {
        //isNan - determines whether a value is Nan (not a number) or a number
        return `Unexpected Input`;
    }

    else if(num === '') {
        return "Please input inside the box";
    }

    else {
        if 
    }
}

for /

/*
asasasasasas
/